Topic: 手环端便签系列全闭环 → Lua 表盘轮（模拟器测试 ✅）→ 清单通知（用户"我们的目的是清单可以通知"→ 方案② 表盘常驻 → **方向升级 = 方案③ 后台保活提醒**）→ **已实现代码 + build ✅，当前卡在 launcher 启动 TickIt（unqlite 注册）**
# Session checkpoint
_Generated by checkpoint writer; structure preserved across updates. Edit only content under italic instructions._

## §1 Active intent
_User's most recent explicit request, verbatim block-quoted from the conversation. The verbatim quote is the ground truth for what to do — do not paraphrase._

> "好"

（Paraphrased：用户确认方案③（清单 App 后台保活 + 截止时间振动提醒）。代码已实现（manifest background:system.request + app.ux 后台服务 keepalive + 截止检查 + vibrator），build 通过。当前唯一卡点 = launcher 启动 TickIt（需往 install_info_v1.db/launcher.db unqlite 写入包注册）。启动后验证后台保活 + 截止振动是否生效。）

## §2 Next concrete action
_The single next concrete step, derived from §1 and current state. Include verbatim quote when the user gave one._

1. 🔄 **launcher 启动 TickIt = 当前唯一卡点**：install_info_v1.db（unqlite）键值格式已确认 = `'com.xxx' => '{"com.xxx":"YYYY.MM.DD HH:MM:SS_version"}'`（JSON + null 终止）；launcher.db 有 layout_v001 + orderlist_v001（应用排序列表）→ **下一步 = Python unqlite 往两个 db 写入 TickIt 条目 → adb push 回模拟器 → reboot → launcher 显示图标**；备选 = `aiot installDbgAndMkp`（hapjs debugger 启动）
2. 🔄 **启动后验证**：① 创建带截止时间任务（2 分钟后）② 切回表盘（后台）③ 等截止 → 观察振动 + toast
3. （挂起）9 Pro 实机侧载验证
4. （挂起）创建/编辑页黑框滚动建议——待用户表态

## §3 Directives (this session)
_This session's specific working style. Project-level rules are in projects/<pid>/MEMORY.md — do not duplicate them here. Only put items that are session-specific._

- 用户明确要求有问题先查记忆（"有什么问题看看记忆"）
- ⚠️ **用户真实目的 = 清单（待办）到截止时间能通知提醒（"我们的目的是清单可以通知。不是这个"）**——Lua 表盘只是候选载体
- ⚠️ 本会话 edit 工具强制"先 Read 再 edit"
- ⚠️ **本机无 git** → 下载 GitHub 仓库一律 curl.exe -L -o zip + Expand-Archive
- ⚠️ **方案③ = 后台保活提醒**（manifest background:system.request + keepalive request.download 60s + setInterval checkDeadlines 60s + vibrator 振动 + todo_reminded 去重）——代码 + build ✅，launcher 启动待解
- **模拟器快应用安装** = 解包 RPK → adb push /data/quickapp/app/<pkg>/ → apps.json + config.json 注册 + reboot；**launcher 图标来源 = launcher.db + install_info_v1.db（unqlite）**——仅 apps.json 不够（TickIt 实证）
- **unqlite 键值格式** = `包名 => '{\\n\\t"包名":\\t"YYYY.MM.DD HH:MM:SS_版本"\\n}\\x00'`（Python unqlite.UnQLite db[k]=v 可写）
- **adb push 大目录 EOF → 分顶层项逐个 push**；**adb pull 到盘符失败 → Set-Location + 相对路径**；**新 bash PATH 不保留 → 全路径 adb**
- **aiot start 交互式选 avd 非交互不可用**；installDbgAndMkp（hapjs debugger）是备选
- See checkpoint-prev-rounds.md (2 entries) — 便签轮 UI 定稿 directives
- See checkpoint-lua-env.md (4 entries) — Lua 表盘轮环境/部署工作法

## §4 Task tree
_Hierarchical view of tasks with current focus marked. Pulled from task tool's DB; one bullet per top-level task with status + summary._

- ✅ T14 清单 App 后台保活 + 截止时间振动提醒（manifest background + keepalive + 定时检查）— 代码实现 + build ✅ 3642ms

## §5 Current work
_Description of what was being done immediately before this checkpoint. Mention specific file paths and code locations._

🔴 **方案③ 后台保活提醒（代码完成 + build ✅，launcher 启动卡点中）**：

**已实现**（2026-08-06）：
- `src/manifest.json`：features 加 `system.request` + `config.background.features: ["system.request"]`
- `src/app.ux`（完整重写）：
  - 导入 storage/prompt/vibrator/request
  - `startBgService()`：onCreate 启动 keepalive + checkDeadlines，setInterval 60s
  - `keepalive()`：`request.download({url:'https://txcn.top/api.php?action=get_notices&fmt=text', filename:'bgka_'+Date.now()})` — 保持 system.request 接口活跃→后台保活
  - `checkDeadlines()`：读 storage todo_items → 遍历未完成 → `parseInt(it.due,10)<=now` → todo_reminded 去重 → vibrator.vibrate({mode:'long'}) + vibrator.start({duration:500,interval:400,count:3}) + prompt.showToast → storage.set todo_reminded
- build ✅：`npm run build` 3642ms → dist/com.txcnstudio.todo.debug.2.5.1.rpk

**当前卡点 = launcher 启动 TickIt**：
- Python unqlite 已读取 install_info_v1.db：55 键，值 JSON+null 终止
- launcher.db 有 layout_v001 + orderlist_v001（应用排序含全部 com.xiaomi.miwear.* + 三方）
- **需往两个 db 都写入 TickIt**（Python unqlite db[k]=v 方式已验证可读，待写入+push 回测）
- 备选：`aiot installDbgAndMkp`（装 hapjs debugger）用 debugger 启动

**上轮已完成**：
- ✅ 清单 App 部署模拟器（RPK 解包 → push → apps.json/config.json 注册 + reboot）
- ✅ Lua 表盘模拟器测试闭环（用户"哦哦，有了"，真机模式绕过 reloader）
- ✅ 清单通知可行性盘点（表盘能力齐全但数据桥不通 → 方向升级方案③）

## §6 Files and code sections
_Files actively being read or modified. List with one-line purpose._

**方案③ 后台保活（本轮工件）**：
- `F:\TXCN清单\src\manifest.json` — features 加 system.request + config.background.features["system.request"]
- `F:\TXCN清单\src\app.ux` — 后台服务入口（keepalive 60s + checkDeadlines 60s + vibrator + todo_reminded 去重）
- `F:\TXCN清单\dist\com.txcnstudio.todo.debug.2.5.1.rpk` — 含后台保活代码的新 build

**模拟器 launcher 数据库（本轮勘察 + Python unqlite 实证）**：
- `F:\inst.db` — install_info_v1.db 拉取副本（unqlite 55 键）
- `F:\launcher.db` — launcher.db 拉取副本（layout_v001 + orderlist_v001）
- 模拟器端：/data/app/system/install_info_v1.db + /data/app/launcher/launcher.db

**模拟器部署工件**：
- `F:\rpk_extract\` — RPK 解包目录
- `F:\emu_apps.json` + `F:\emu_config.json` — 注册副本
- 模拟器端：/data/quickapp/app/com.txcnstudio.todo/（已推）

**便签项目（已交付）**：
- src/pages/home/home.ux / notecreate.ux / noteedit.ux / notes.ux / about.ux
- src/common/logo.png + 便签素材；utils.js；qrcode.ux；confirm.ux；manifest.json（2.5.1/4）

**Lua 表盘**：
- F:\LuaDevTemplate\LuaDevTemplate-main\watchface\fprj\app\lua\main.lua（466x466 真机模式）
- F:\LuaDevTemplate\LuaDevTemplate-main\watchface\fprj\LuaDevTemplate.fprj（466x466 DeviceType=362）

## §7 Discovered knowledge (cross-task)
_Facts learned during this session that may apply to future tasks._

- ✅ **快应用 manifest 支持 background 配置（2026-08-06 实证）**：`config.background.features` 数组可申请 `system.audio`/`system.geolocation`/`system.request`；声明 + 接口正在运行（持续 request.download）→ 切后台保持运行；**保活工作放 app.ux**（文档建议）
- ✅ **快应用 vibrator API（9 Pro 支持）**：`vibrator.vibrate({mode:'long'|'short'})` + `vibrator.start({duration:interval:count:})` 连续振动 + `vibrator.stop(id)`；提醒用长振+连续 3 次组合
- ✅ **system.request = request.download（下载接口）**，非 fetch；保活需用 `request.download()` 周期触发；system.fetch 是另一个模块
- ✅ **install_info_v1.db unqlite 格式（2026-08-06 Python unqlite 实证）**：`unqlite.UnQLite('F:/inst.db')` 打开；55 键=包名，值=`'{\\n\\t"包名":\\t"YYYY.MM.DD HH:MM:SS_版本"\\n}\\x00'`（JSON + null 终止符）；Python 写入 = `db[k] = v`（byte/str）；launcher.db 同格式但含 layout_v001 + orderlist_v001
- ✅ **快应用无 alarm/calendar API（docs-quickapp 完整功能列表无匹配）**——系统级提醒只能通过保活自身实现
- ✅ **Vela 系统自带 com.xiaomi.miwear.todolist（待办应用）**——在 install_info_v1.db 注册，launcher 可见；数据/提醒机制未知但不影响自研方案
- See checkpoint-notes-history.md (21 entries) — 便签轮历史已完成知识
- See checkpoint-prev-rounds.md (2 entries) — UI 定稿 + Lua 表盘轮遗留

## §8 Errors and fixes
_Errors encountered this session and how they were resolved. Newest first._

- 🔧 **launcher 无 TickIt 图标（进行中，未解决）**：apps.json/config.json 注册 + reboot 后 launcher 无图标 → 勘察 launcher 数据源 = launcher.db + install_info_v1.db（unqlite）；Python unqlite 库 `db.keys()` 返回 generator → 用 `list(db.keys())` 包装；db[k] 返回 bytes → decode；键值格式已确认 → 下一步写入
- 🔧 **aiot start 交互式非交互 ExitPromptError**：选 avd prompt 在非交互 shell 退出 → 改手动 push 注册
- 🔧 **Python unqlite 库 open/keys API 差异**：`unqlite.open()` 不存在（AttributeError）→ 改用 `unqlite.UnQLite()`；`db.keys()` 返回 generator → `list(db.keys())`；`db[k]` 返回 bytes → `.decode()` 或 str 直接用
- See §8 historical (14 entries) — Expand-Archive/adb push EOF/pull 盘符/reloader.lua/fprj 尺寸/GitHub 超时/NuttX shell 限制/PATH 不保留/PowerShell 解析

## §9 Live resources
_Runtime state: branch, uncommitted files, running processes, temp artifacts._

- cwd：F:\TXCN清单（非 git）；Lua 表盘工作目录 = F:\LuaDevTemplate\LuaDevTemplate-main\
- **Vela 模拟器在线**：emulator-5554（NuttX）；表盘 466x466 真机模式已部署
- **Python 3.12.10** + unqlite 库已装（pip install unqlite）→ 可读写 unqlite 数据库
- 模拟器端已推：/data/quickapp/app/com.txcnstudio.todo/ + apps.json/config.json 注册
- 本地：F:\inst.db + F:\launcher.db（拉取副本）+ F:\rpk_extract\ + F:\emu_apps.json + F:\emu_config.json
- RPK 交付：F:\TXCN清单\dist\com.txcnstudio.todo.release.2.5.1.rpk（449799B）+ debug.2.5.1.rpk（含后台保活）
- adb 全路径：C:\Users\Administrator\.aiot-ide\extensions\vela.aiot-emulator-1.7.22\dist\bin\win\adb.exe
- 无运行中进程/服务

## §10 Design decisions and discussion outcomes
_Decisions reached through discussion that produced no immediate code/file artifact._

- ✅ **方案③ 后台保活提醒定案（2026-08-06 用户确认"好"）**：manifest background:system.request + 周期 request.download 保活 + setInterval checkDeadlines 检查 todo_items due → vibrator 振动 + toast 提醒 + todo_reminded 去重；**切后台仍提醒**（保活条件满足）；**彻底退出/关机失效**（平台限制）
- ✅ **方案②（表盘常驻）基本放弃**：数据桥实证结论 = 快应用 storage 落盘 unqlite KV 私有存储，表盘无法解析；快应用 file API 仅 internal:// 私有；共享路径无 API → 数据桥不通 → 方向升级为方案③
- ✅ **表盘模拟器验证闭环 + 真机模式定案（用户"哦哦，有了"）**：S4 模拟器 466x466；reloader.lua 不兼容（log_error 缺失崩溃）→ 真机模式：push main.lua + rm _app_main.lua + reboot
- ✅ **DeviceType 不阻塞交付定案**：.face 无 DeviceType 字段 → 侧载不依赖；DeviceType 362 编译的 .face 可侧载 9 Pro
- ✅ **表盘设计定案**：时间+日期，dataman+os.date 兜底，montserrat 32px，简约深色
- ✅ **省略号/截断根治定案**：div auto 高度 + min-height:112 + text 不设 lines
- ✅ **完成项对勾居中定案**：left:38/top:33
- ✅ **关于页图标定案**：logo.png 64x64 + V2.5.1
- ✅ **删除确认页背景 = sanc.png（2026-08-04 用户指定）**
- See §10 historical (8 entries) — UI 定稿/拆页/保存不卡/光标/字号/清理/黑框自适应

## §11 Open notes
_Writer-curated catch-all for items that don't fit §1-§10._

- 🔄 **launcher 启动 TickIt 待解（当前卡点）**：install_info_v1.db + launcher.db 均需写入 TickIt（Python unqlite 可写 db[k]=v）；备选 = aiot installDbgAndMkp（hapjs debugger 启动）；写入后 adb push 回 + reboot → 验证 launcher 图标
- 🔄 **后台保活验证待做**：需启动 TickIt → 创建带截止任务 → 切后台 → 观察振动/toast
- ⚠️ **后台保活方案不确定性**：Vela 是否真让声明 request 的 App 后台保活（文档确认但未实测）；后台时 setInterval 精度/执行；request.download 写文件累积；耗电（自用不审核）
- ⚠️ **创建/编辑页黑框滚动建议**：当前 200 字约 60-70 字可见；用户未明确表态
- dist 现有 debug.2.5.1.rpk（含后台保活）+ release.2.5.1.rpk（旧版无保活）

### Execution context
- cwd：F:\TXCN清单（非 git）；Lua 表盘工作目录 = F:\LuaDevTemplate\LuaDevTemplate-main\
- adb：C:\Users\Administrator\.aiot-ide\extensions\vela.aiot-emulator-1.7.22\dist\bin\win\adb.exe
- Python 3.12.10 + unqlite 库已装
- 模拟器：emulator-5554（NuttX，S4 466x466）
